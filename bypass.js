// bypass.js - "Totally Legal" 18+ Bypass
function checkPerversionLevel() {
  if (window.ethereum && window.ethereum.selectedAddress) {
    return "verified_degensitate"; 
  }
  localStorage.setItem("adulthood", "true"); // 😉
}
